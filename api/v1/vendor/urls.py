from django.urls import path
from vendor import views

app_name = "vendor"

urlpatterns = [
    path("",views.index, name="index"),
    path("",views.index, name="index"),
    path("",views.index, name="index"),
    path("",views.index, name="index"),
    path("",views.index, name="index"),

    ]